import { test,expect } from "@playwright/test";
test("client workflow is accessible",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/Find every lawful tax benefit/})).toBeVisible();const start=page.getByRole("link",{name:/Start tax profile/});await expect(start).toHaveAttribute("href","/start");await Promise.all([page.waitForURL("**/start"),start.click()]);await expect(page.getByRole("heading",{name:/start with your tax profile/i})).toBeVisible();await expect(page.getByRole("checkbox")).not.toBeChecked()});
test("PAN extraction displays identity details below the upload area",async({page})=>{await page.addInitScript(()=>localStorage.setItem("taxright.firebase.idToken","test-token"));await page.route("**/api/documents/extract",async route=>route.fulfill({status:200,contentType:"application/json",body:JSON.stringify({extraction:{detectedDocumentType:"PAN_CARD",confidence:.99,identity:{fullName:"RAMAMURTHY VALAVANDAN",dateOfBirth:"1972-08-19",pan:"AHHPR0360C",aadhaarLast4:null},income:{employerName:null,employeeId:null,payPeriod:null,grossEarningsForPeriod:null,annualGrossSalary:null,taxableIncome:null,tdsForPeriod:null,annualTds:null},warnings:[],model:"test-model"}})}));await page.goto("/start");await page.getByRole("checkbox").check();await page.locator('input[type="file"]').first().setInputFiles("docs/TaxRight_Test_Mastech_Digital_TY2026-27_JUL2026.pdf");await expect(page.getByText(/TaxRight_Test_Mastech_Digital/)).toBeVisible();await page.getByRole("button",{name:"Extract PAN details"}).click();const result=page.getByRole("region",{name:"Details from your uploaded PAN card"});await expect(result).toBeVisible();await expect(result).toContainText("RAMAMURTHY VALAVANDAN");await expect(result).toContainText("19-08-1972");await expect(result).toContainText("AHHPR0360C");await expect(page.getByLabel("Date of birth")).toHaveValue("1972-08-19")});
test("income and deductions forms expose requested evidence fields",async({page})=>{await page.goto("/case/demo/income");await expect(page.getByRole("heading",{name:"Salary evidence"})).toBeVisible();await expect(page.getByLabel("Annual gross salary")).toBeVisible();await page.goto("/case/demo/deductions");await expect(page.getByLabel("Groceries & household")).toBeVisible();await expect(page.getByLabel("Gold savings scheme")).toBeVisible();await expect(page.getByRole("button",{name:/80C investments & savings/})).toHaveAttribute("aria-expanded","true");await expect(page.getByLabel("LIC / life-insurance premium")).toBeVisible()});
test("client surface blocks staff routes",async({page})=>{const response=await page.goto("/staff");expect(response?.status()).toBe(404);await expect(page.locator("body")).not.toContainText(/Tax review workspace/)});
