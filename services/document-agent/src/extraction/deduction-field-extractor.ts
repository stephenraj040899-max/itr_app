import type {DocumentType,ProvenanceField} from "../shared/types.js";
const amount=(text:string):number|null=>{const labels=/(?:premium|contribution|tuition(?:\s+fee)?|interest(?:\s+paid)?|amount(?:\s+paid)?|total(?:\s+paid)?)[^\d]{0,40}(?:₹|rs\.?|inr)?\s*([\d,]+(?:\.\d{1,2})?)/ig;let best=0;for(const match of text.matchAll(labels)){const n=Number(match[1]!.replace(/,/g,""));if(Number.isSafeInteger(Math.round(n))&&n>best)best=Math.round(n)}return best||null};
const make=(name:string,value:number|null,documentId:string):ProvenanceField=>({name,value,confidence:value===null?0:.9,sourceDocumentId:documentId,sourcePage:null,sourceRegion:null,extractionMethod:"DETERMINISTIC",extractorName:"deduction-field-extractor",extractorVersion:"1.0.0",validated:value!==null,reviewStatus:value===null?"REVIEW_REQUIRED":"DETECTED"});
export function extractDeductionFields(text:string,documentId:string,type:DocumentType):ProvenanceField[]{const supported:DocumentType[]=["LIFE_INSURANCE_PREMIUM","HEALTH_INSURANCE_PREMIUM","TUITION_FEE_RECEIPT","EPF","PPF","ELSS","NSC","NPS_CONTRIBUTION","BANK_INTEREST_CERTIFICATE","HOME_LOAN_PRINCIPAL_CERTIFICATE","EDUCATION_LOAN_INTEREST","DONATION_80G"];if(!supported.includes(type))return [];return [make("deduction_amount_rupees",amount(text),documentId)];}
