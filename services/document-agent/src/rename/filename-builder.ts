import type { DocumentType } from "../shared/types.js";
import { issuerSlug } from "./issuer-slug.js";
const labels:Partial<Record<DocumentType,string>>={FORM_16:"FORM16",FORM_16_PART_A:"FORM16A",FORM_16_PART_B:"FORM16B",SALARY_SLIP:"SALARYSLIP",HEALTH_INSURANCE_PREMIUM:"80D",TUITION_FEE_RECEIPT:"TUITION",HOME_LOAN_CERTIFICATE:"HOMELOAN",DONATION_80G:"80G"};
export function buildFilename(i:{clientId:string;assessmentYear:string;documentType:DocumentType;issuer:string|null;documentDate:string|null;sequence:number;extension:string;fallbackDate:string}):string{const short=i.clientId.replace(/-/g,"").slice(-6).toUpperCase();const ay=i.assessmentYear.startsWith("AY")?i.assessmentYear:`AY${i.assessmentYear}`;const date=(i.documentDate??i.fallbackDate).replace(/-/g,"");const ext=i.extension.toLowerCase().replace(/[^a-z0-9]/g,"");return `TRAI_${short}_${ay}_${labels[i.documentType]??i.documentType}_${issuerSlug(i.issuer)}_${date}_${String(i.sequence).padStart(2,"0")}.${ext}`;}
export function categoryFolder(t:DocumentType):string{if(["FORM_16","FORM_16_PART_A","FORM_16_PART_B","SALARY_SLIP"].includes(t))return "income";if(["AIS","TIS","FORM_26AS"].includes(t))return "income-tax-records";if(t.includes("INSURANCE"))return "insurance";if(t==="TUITION_FEE_RECEIPT"||t==="EDUCATION_LOAN_INTEREST")return "education";if(t.includes("HOME_LOAN"))return "housing";if(t.includes("RENT"))return "rent";if(t==="DONATION_80G"||t==="FORM_10BE")return "donations";if(t.includes("MEDICAL")||t==="DISABILITY_CERTIFICATE")return "medical";if(t==="TAX_PAYMENT_CHALLAN")return "tax-payments";if(t==="ITR_ACKNOWLEDGEMENT")return "acknowledgements";return "other";}
