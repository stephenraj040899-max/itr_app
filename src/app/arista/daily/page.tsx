import Link from "next/link"; import { dailyPlans } from "@/lib/arista/catalog";
export default function Daily(){return <main className="page"><div className="eyebrow">Arista Daily</div><h1>One plan. Every day covered.</h1><p className="lead">Pick a box designed around your day. Test checkout for the demo is clearly separated from live franchise payments.</p><div className="arista-grid plans">{dailyPlans.map(p=><article className="plan-card" key={p.id}><span className="eyebrow">{p.audience}</span><h2>{p.name}</h2><strong>₹{p.price}<small>/ day</small></strong><p>About ₹{p.price*30} monthly</p><div className="callout">{p.contents}</div><Link className="button primary" href={`/arista/daily/success?plan=${p.id}`}>Select {p.name}</Link></article>)}</div><div className="actions"><Link className="button secondary" href="/arista">Back to Arista</Link></div></main>}
