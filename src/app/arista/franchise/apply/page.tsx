"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import Link from "next/link";
export default function Apply(){const router=useRouter();const [busy,setBusy]=useState(false);const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const data=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch("/api/franchise/nominate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const b=await r.json();if(!r.ok)throw new Error(b.error?.message||"Could not save nomination");router.push(`/arista/franchise/apply?nomination=${encodeURIComponent(b.nominationId)}`);}catch(x){setMessage(x instanceof Error?x.message:"Could not save nomination");setBusy(false)}}const nomination=typeof window!=="undefined"?new URLSearchParams(window.location.search).get("nomination"):null;return <main className="page"><div className="eyebrow">Arista franchise · nomination</div><h1>Nominate your territory.</h1><p className="lead">Tell us where you want to build. You can pay the token now or choose pay later — a failed payment never loses your lead.</p>{nomination?<section className="surface panel"><div className="eyebrow">Nomination saved</div><h2>Choose your next step</h2><p className="muted">Your nomination reference is <strong>{nomination}</strong>.</p><div className="actions"><Link className="button primary" href={`/arista/franchise/apply/pay?nomination=${nomination}`}>Pay token advance ₹25,000</Link><Link className="button secondary" href="/arista/franchise">Pay later</Link></div><p className="fine">This token advance reserves your nomination for one Arista franchise territory. It is adjustable against the franchise fee and refundable as per the terms provided. Paying a token does not by itself confer a franchise; allotment follows evaluation and agreement signing.</p></section>:<form className="surface panel" onSubmit={submit}><div className="form-grid"><div className="field"><label htmlFor="name">Full name</label><input className="input" id="name" name="name" required/></div><div className="field"><label htmlFor="phone">Phone</label><input className="input" id="phone" name="phone" pattern="[6-9][0-9]{9}" inputMode="numeric" required/></div><div className="field"><label htmlFor="email">Email</label><input className="input" id="email" name="email" type="email" required/></div><div className="field"><label htmlFor="city">City</label><input className="input" id="city" name="city" required/></div><div className="field"><label htmlFor="pincode">Preferred pincode</label><input className="input" id="pincode" name="pincode" pattern="[0-9]{6}" required/></div><div className="field"><label htmlFor="area">Area / locality</label><input className="input" id="area" name="area" required/></div><fieldset className="field"><legend>Investment readiness</legend><label><input type="radio" name="investmentReady" value="NOW" required/> Ready now</label><label><input type="radio" name="investmentReady" value="EXPLORING"/> Exploring</label><label><input type="radio" name="investmentReady" value="LATER"/> Later</label></fieldset></div><label className="checkbox-row"><input type="checkbox" name="consent" value="true" required/><span>I consent to Nature Labs contacting me about this nomination and agree to the <Link href="/legal/terms">terms</Link>.</span></label>{message?<p className="form-error" role="alert">{message}</p>:null}<div className="actions"><Link className="button secondary" href="/arista/franchise">Back</Link><button className="button primary" disabled={busy}>{busy?"Saving…":"Submit nomination"}</button></div></form>}</main>}
