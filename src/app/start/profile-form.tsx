"use client";
import Link from "next/link";
import {useRouter} from "next/navigation";
import {useState} from "react";
import {ArrowRight,LockKeyhole} from "lucide-react";
import {currentIdToken} from "@/lib/auth/client";
export function ProfileForm(){const router=useRouter(),[consent,setConsent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();if(!consent)return;setBusy(true);setError("");try{const data=new FormData(event.currentTarget),token=await currentIdToken();const response=await fetch("/api/cases",{method:"POST",headers:{authorization:`Bearer ${token}`,"content-type":"application/json"},body:JSON.stringify({profile:{fullName:data.get("fullName"),dateOfBirth:data.get("dateOfBirth"),pan:data.get("pan"),email:data.get("email"),mobile:data.get("mobile"),assessmentYear:data.get("assessmentYear")},processingConsent:{accepted:true,consentVersion:"tax-processing-v1",noticeVersion:"privacy-v1"}})}),payload=await response.json();if(!response.ok)throw new Error(payload.error?.message??"Could not create your case.");router.push(`/case/${payload.caseId}/income`)}catch(reason){setError(reason instanceof Error?reason.message:"Could not create your case.")}finally{setBusy(false)}}return <form onSubmit={submit}><div className="form-grid"><div className="field"><label htmlFor="name">Full name</label><input className="input" id="name" name="fullName" autoComplete="name" required/><span className="fine">Enter the name exactly as shown on your PAN.</span></div><div className="field"><label htmlFor="dob">Date of birth</label><input className="input" id="dob" name="dateOfBirth" type="date" max={new Date().toISOString().slice(0,10)} required/></div><div className="field"><label htmlFor="pan">PAN</label><input className="input" id="pan" name="pan" inputMode="text" autoCapitalize="characters" maxLength={10} pattern="[A-Za-z]{5}[0-9]{4}[A-Za-z]" aria-describedby="pan-help" required/><span className="fine" id="pan-help">Used server-side to prevent duplicate profiles; not used in URLs or filenames.</span></div><div className="field"><label htmlFor="ay">Assessment year</label><select className="select" id="ay" name="assessmentYear" defaultValue="AY2026-27"><option>AY2026-27</option></select></div><div className="field"><label htmlFor="email">Email address</label><input className="input" id="email" name="email" type="email" autoComplete="email" required/></div><div className="field"><label htmlFor="mobile">Mobile number</label><input className="input" id="mobile" name="mobile" type="tel" inputMode="numeric" autoComplete="tel" pattern="[6-9][0-9]{9}" placeholder="10-digit Indian mobile" required/></div><div className="field full"><div className="callout"><div style={{display:"flex",gap:10,alignItems:"center",fontWeight:750}}><LockKeyhole size={17}/>Your information stays private</div><p className="fine">We use your documents only for tax assessment, evidence analysis and human review under the notices you accept.</p><div style={{display:"flex",gap:14,flexWrap:"wrap"}}><Link href="/legal/privacy">Privacy Notice</Link><Link href="/legal/terms">Terms of Service</Link><Link href="/legal/processing">Data Processing Notice</Link></div></div></div><div className="field full"><label className="checkbox-row"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)} required/><span>I consent to TaxRight AI processing the personal, financial and tax documents I provide for tax assessment, deduction analysis, document preparation and human review. I confirm that the information and documents submitted by me are genuine and accurate to the best of my knowledge.</span></label></div></div>{error&&<div className="callout warning" role="alert">{error}</div>}<div className="actions"><button className="button primary" type="submit" disabled={!consent||busy}>{busy?"Creating secure case…":"Continue"}<ArrowRight size={17}/></button></div></form>}
