"use client";
import Link from "next/link";
import { useState } from "react";
import { Gem, ShoppingBasket, Wrench } from "lucide-react";
const choices=[["GROCERY",ShoppingBasket,"Grocery","Everyday household grocery support"],["SERVICES",Wrench,"Services","Household services, bills and related assistance"],["GOLD",Gem,"Gold","Gold-related services and planning"]] as const;
export function PreferenceForm(){const [choice,setChoice]=useState("");const [ok,setOk]=useState(false);const ready=Boolean(choice&&ok);return <form onSubmit={e=>e.preventDefault()}><fieldset style={{border:0,padding:0,margin:0}}><legend className="field-label">Choose one preference</legend><div className="radio-grid">{choices.map(([value,Icon,title,text])=><label className="radio-card" key={value}><input type="radio" name="preference" value={value} onChange={()=>setChoice(value)}/><Icon/><strong>{title}</strong><span className="fine">{text}</span></label>)}</div></fieldset><label className="checkbox-row"><input type="checkbox" checked={ok} onChange={e=>setOk(e.target.checked)}/><span>I confirm that I have reviewed the information above and selected this preference voluntarily. I understand that this preference is separate from any tax-deductible donation or deduction calculation and does not by itself create entitlement to a tax benefit.</span></label><div className="actions"><Link className="button secondary" href="/case/demo/80g">Back</Link><Link className="button primary" href={ready?"/case/demo/review":"#"} aria-disabled={!ready} tabIndex={ready?0:-1}>Confirm & Proceed</Link></div></form>}
