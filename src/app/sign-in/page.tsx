"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { signIn } from "@/lib/documents/client";
export default function SignInPage(){ const router=useRouter(); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [create,setCreate]=useState(false); const [message,setMessage]=useState(""); async function submit(e:FormEvent){e.preventDefault();setMessage("Signing in…");try{await signIn(email,password,create);router.push("/start");}catch(err){setMessage(err instanceof Error?err.message:"Sign-in failed");}} return <main className="page"><section className="surface panel" style={{maxWidth:520,margin:"64px auto"}}><div className="eyebrow">Secure client access</div><h1>{create?"Create your TaxRight account":"Sign in to TaxRight AI"}</h1><p className="fine">A secure Firebase ID token is required before sensitive tax documents can be processed.</p><form onSubmit={submit}><div className="field"><label htmlFor="email">Email address</label><input className="input" id="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label htmlFor="password">Password</label><input className="input" id="password" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></div>{message?<p className="form-error" role="alert">{message}</p>:null}<button className="button primary" type="submit">{create?"Create account":"Sign in"}</button></form><button className="button secondary" type="button" onClick={()=>{setCreate(!create);setMessage("")}}>{create?"Already have an account? Sign in":"New client? Create an account"}</button><p className="fine"><Link href="/">Back to TaxRight AI</Link></p></section></main> }
